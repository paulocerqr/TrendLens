# PostgreSQL container image. Pin the major version for reproducibility.
POSTGRES_IMAGE=postgres:16-alpine

# Non-secret database identifiers.
POSTGRES_DB=trendlens
POSTGRES_USER=trendlens_app

# Replace this value in .env before starting the container.
POSTGRES_PASSWORD=replace_with_a_strong_password

TZ=America/Sao_Paulo
